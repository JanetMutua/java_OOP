package java_interface;

// meant to set certain specifications/ contract to a class
interface BicycleInterface {

    //declare methods and add arguments in it then implement with a child class

    void addBrake(int reduceSpeed);
    void accelerate(int addSpeed);
}
